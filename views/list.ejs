<!-- <!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do List</title>
    <link rel="stylesheet" href="css/styles.css">
</head>

<body> -->

    <%- include("header") -%>

    <div class="box" id="heading">
        <h1><%= listTitle %></h1>
    </div>

    <div class="box">

       <% newListItems.forEach(function(item){ %>
        <form action="/delete" method="post">    
            <div class="item">
                <input name="checkbox" value="<%= item._id %>" onChange="this.form.submit()" type="checkbox">
                <p> <%= item.name %> </p>
            </div> 
            <input type="hidden" name="listName" value="<%= listTitle %>"></input>
        </form>
            <%  }) %>

        <!-- 2nd for loop loops through the entire lenght of the array and renders a new li for each item inside the array -->

        <form class="item" action="/" method="post">
            <!-- when submit button clicked, form makes post request to the home route and post "newItem" -->
            <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
            <button type="submit" name="list" value=<%= listTitle %>>+</button>
        </form>
        <!-- 3rd when the user adds a new item, it gets saved as "newItem" and we trigger a post request to the home route -->
    </div>

    <%- include("footer") -%>
<!-- 
</body>

</html> -->